[Unit]
Description=Kiwi IRC Web Client (Static Files)
# Este serviço não é necessário pois o KiwiIRC é servido pelo Nginx como arquivos estáticos
# Mantido apenas para referência/compatibilidade

[Service]
Type=oneshot
ExecStart=/bin/true
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target